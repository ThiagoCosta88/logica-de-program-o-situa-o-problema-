let PesoPecas = [ PesoPecas >= 100];
console.log(PesoPecas);
let quantidadecaixa = [quantidadecaixa <= 10];
console.log(quantidadecaixa);
let quantidadecaracteres = [quantidadecaracteres.length > 3];
if (PesoPecas >= 100("produto cadastrado com sucesso") ){
  // TODO document why this block is empty

}else {
    alert("cadastro negado peso incorreto");
}
if (quantidadecaixa <= 10("pedido finalizado com sucesso")){
  // TODO document why this block is empty

}else {
   alert("quantidade excede o limite da caixa");
}
if (quantidadecaracteres.length > 3){
  // TODO document why this block is empty

}else{ 
alert("Não é permitido esse nome");
    
}

